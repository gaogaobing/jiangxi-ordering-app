version: '3.8'

services:
  app:
    build: .
    container_name: jiangxi-ordering
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/public/uploads
    restart: unless-stopped
    environment:
      - PORT=3000
      - NODE_ENV=production
